import XCTest
@testable import WifiHeatmap

final class WifiHeatmapTests: XCTestCase {

    func testExample() throws {
        // Placeholder test — add real tests as the app grows.
        XCTAssertTrue(true)
    }

}
